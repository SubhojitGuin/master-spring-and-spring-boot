FROM openjdk:21-ea-jdk-slim
COPY target/*.jar app.jar
EXPOSE 5000
ENTRYPOINT ["java", "-jar", "/app.jar"]

#This dockerfile has a problem, the jar files are created in the local machine, so it
#can be different for different machines and may cause a problem